# Copy to /opt/reloop/ml-service/.env  (referenced by reloop-ml.service)
# Must include your public https origin, or the vision-stream WS is rejected.
ALLOWED_ORIGINS=https://SERVER_NAME_PLACEHOLDER
# Optional: COCO classes to hide from overlay (default already excludes person)
EXCLUDED_CLASSES=person
